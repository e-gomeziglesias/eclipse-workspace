/*
 ============================================================================
 Name        : Emiliano Gaston Gomez Iglesias
 Author      : 
 Version     :
 Copyright   : Your copyright notice
 Description : Ejercicio 2-1: Ingresar 5 números enteros
 calcular y mostrar el promedio de los números. Probar la
 aplicación con distintos valores.

 ============================================================================
 */

#include <stdio.h>
#include <stdlib.h>

int main()
{
	setbuf(stdout,NULL);

    int numero[4];
    int suma=0;
    int i;

    float promedio;

    printf("Calcular el promedio\n\n");

    for(i=0; i<5; i++)
    {
        printf("Ingrese el %d°numero: ",i+1);
        scanf("%d", &numero[i]);
        suma = suma + numero[i];
    }

    /*Parseo de datos:
     * Implica convertir una cadena (string) en un tipo de
     * dato numérico.
     *
     * Casteo de datos:
     * Implica convertir un tipo de dato numérico en otro
     * tipo de datos numérico.
     *
     * Casteo implícito
     * int x;
     * float y;
     * y = 3.25;
     * x = y;
     *
     *
     * Casteo explícito
     *
     * int x;
     * float y;
     *
     * y = 3.25;
     * x = (int) y;
     */

    promedio = (float)suma/i; /*casteo explícito.
    Convierto el valor que
    devuelve suma a flotante, no la variable y luego lo divido por i.*/

    printf("\n\nEl promedio es %.2f ", promedio);

	return EXIT_SUCCESS;
}
